{
  "name": "trello-clone",
  "version": "1.0.0",
  "description": "This is the clone of the trello project management app with limited functionalities for now. We are using trello api for this project.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^8.2.0"
  }
}
